#[test]
fn simple_sql() {
    // let dialect = GenericDialect {}; // or AnsiDialect
    //
    // let sql = "SELECT a, b \
    //        FROM table_1 \
    //        WHERE a > b AND b < 100";

    // let ast = Parser::parse_sql(&dialect, sql).unwrap();
}
